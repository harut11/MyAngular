APP.controller('AdminDashboardController',function($scope,$stateParams,toastr,AuthService,$state, ProductService, CategoryService) {
	// $scope.products = [];
	// $scope.pagination = {};

	// $scope.getProducts = function(page) {
	// 	ProductService.get({page: page}, (res) => {
	// 		$scope.products = res.products.data;

	// 		$scope.pagination = {
	// 			last_page   : new Array(res.products.last_page),
	// 			currentPage : res.products.current_page
	// 		}
	// 	})
	// }
	// $scope.getProducts(1);
});